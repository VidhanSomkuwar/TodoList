/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export', // This tells Next.js to export the site as static HTML
    // basePath: '/TodoList', // Replace with the name of your GitHub repository
    // assetPrefix: '/TodoList/', // Same as the basePath
  };
  
  export default nextConfig;
  